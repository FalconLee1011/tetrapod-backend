# tetrapod-backend

## Prerequisite
- python
- pyenv (opt)
- pipenv

## Installation
### if you are NOT lazy, run
`mkdir .venv` (opt)

`pipenv install`

`mkdir cache`


## Run
`pipenv run python -m tetrapod_backend`

